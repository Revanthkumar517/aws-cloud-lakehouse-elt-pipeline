name: aws_lakehouse_dbt_project
version: '1.0.0'
config-version: 2

profile: aws_lakehouse_dbt_project

model-paths: ["models"]
macro-paths: ["macros"]

models:
  aws_lakehouse_dbt_project:
    staging:
      +materialized: view
    marts:
      +materialized: table
